# Postgres (Supabase or Railway PG). Prisma reads DATABASE_URL.
# Use a pooled URL for the app and a direct URL for migrations if your host needs it.
DATABASE_URL="postgresql://user:password@localhost:5432/itsolute_connect?schema=public"
